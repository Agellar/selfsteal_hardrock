# Iron Wave Radio — Hard Rock Online

Лёгкий статический сайт-витрина онлайн-радио в жанре **Hard Rock**. Объединяет
несколько публичных интернет-радиопотоков в одном плеере и дополнен
просветительскими разделами (история жанра, артисты, поджанры, гид для
новичков). Без бэкенда, без сборки, без внешних зависимостей во время выполнения.

> Проект также используется как **selfsteal**-маскировка (decoy-сайт для
> Reality/VLESS). Все ресурсы самодостаточны и отдаются с одного origin, чтобы
> страница выглядела как обычный живой сайт и не делала лишних внешних запросов.

## Структура

```
.
├── index.html          # Главная: плеер, сетка станций, тизеры разделов, факты
├── history.html        # Полная история жанра (хронология)
├── artists.html        # Профили легендарных групп
├── subgenres.html      # Путеводитель по поджанрам тяжёлой музыки
├── guide.html          # Как слушать интернет-радио (FAQ для новичков)
├── about.html          # О проекте + Политика конфиденциальности + Cookie (152-ФЗ)
├── robots.txt          # Разрешает индексацию, ссылается на sitemap
├── sitemap.xml         # Карта сайта (6 страниц) для поисковиков
├── assets/
│   ├── styles.css      # Все стили (общие для всех страниц)
│   ├── app.js          # Плеер, данные станций, тема, меню, cookie-баннер
│   ├── fonts.css       # @font-face → локальные woff2 (self-hosted)
│   └── fonts/          # Barlow / Barlow Condensed (.woff2), без Google Fonts
└── README.md
```

Общий «каркас» (шапка, плеер, подвал) продублирован в каждом HTML-файле — это
осознанный выбор: сайт остаётся чисто статическим и не требует шаблонизатора
или JS-роутера. Логика и стили вынесены в `assets/` и не дублируются.

## Радиостанции

12 потоков от публичных вещателей. Все проверены на доступность по **HTTPS** и
воспроизведение **из стран СНГ без VPN** (источники — Германия и США, без
региональной геоблокировки). Список и URL потоков задаются в массиве `STATIONS`
в начале `assets/app.js`.

| Станция | Стиль | Вещатель | Битрейт |
|---|---|---|---|
| Rock Antenne | Classic / Hard Rock | 🇩🇪 | MP3 128k |
| Rock Antenne Heavy Metal | Heavy / Thrash | 🇩🇪 | MP3 128k |
| RA Classic Perlen | Classic Rock | 🇩🇪 | MP3 128k |
| RA Alternative | Alternative / Grunge | 🇩🇪 | MP3 128k |
| RA Punk Rock | Punk / Hardcore | 🇩🇪 | MP3 128k |
| 181.fm The Rock! | Hard Rock | 🇺🇸 | MP3 128k |
| 181.fm Hard Rock | Hard Rock / Metal | 🇺🇸 | MP3 128k |
| 181.fm Awesome 80s | Glam / Hair Metal | 🇺🇸 | MP3 128k |
| 181.fm Power Hits | Rock Hits | 🇺🇸 | MP3 128k |
| 0N Rock | Rock | 🇩🇪 | MP3 192k |
| 0N Classic Rock | Classic Rock | 🇩🇪 | MP3 192k |
| laut.fm Metal | Metal | 🇩🇪 | MP3 128k |

Плеер сам подключает следующий поток, если текущий не отвечает в течение 12
секунд или возвращает ошибку.

### Как добавить или заменить станцию

Откройте `assets/app.js`, найдите массив `STATIONS` и добавьте объект:

```js
{ name: "Название", emoji: "🎸", country: "🇩🇪", bitrate: "MP3 128k",
  description: "Краткое описание",
  genres: ["Hard Rock", "Metal"],
  url: "https://example.com/stream.mp3", isLive: true }
```

Требования к потоку: прямой URL аудио (MP3/AAC), **только HTTPS** (иначе браузер
заблокирует mixed content на HTTPS-странице), доступность из целевого региона.

## Запуск локально

Это статика — достаточно любого HTTP-сервера:

```bash
python3 -m http.server 8080
# затем откройте http://localhost:8080
```

Открытие через `file://` тоже работает, но рекомендуется HTTP, чтобы корректно
отрабатывали относительные пути и политика воспроизведения аудио.

### Домен и контакты

Сайт настроен на домен **https://hardrock.legendaryfm.uk/** — он прописан в
`robots.txt`, `sitemap.xml`, тегах `<link rel="canonical">` и `og:url`.
Контактный адрес оператора — **info@legendaryfm.uk** (в подвале и в Политике
конфиденциальности).

Если домен сменится, обновите его одной командой и поправьте canonical-теги:

```bash
sed -i 's#https://hardrock.legendaryfm.uk#https://НОВЫЙ-ДОМЕН#g' \
  robots.txt sitemap.xml *.html
```

## Деплой на сервер (1 команда, рядом с remnanode)

Сценарий — selfsteal: статический сайт отдаётся по **настоящему TLS** на
`127.0.0.1:9443`, а xray Reality на `:443` указывает на него через
`dest/target = 127.0.0.1:9443`. Весь non-Reality трафик (браузеры, активные
проберы) попадает на реальный сайт с валидным сертификатом — в этом и смысл
маскировки.

**Предусловие:** на сервере уже установлен и работает **remnanode** (Docker), а
DNS-запись домена указывает на этот сервер. Reality-инбаунд (serverName + dest)
настраивается в панели remnawave — это ваш шаг, установщик его не трогает.

Интерактивная установка в один клик:

```bash
git clone https://github.com/Agellar/selfsteal_hardrock.git
cd selfsteal_hardrock
sudo bash deploy/install.sh
```

`deploy/install.sh` спросит домен, порт (по умолчанию `9443`) и e-mail для
Let's Encrypt, после чего сам:

1. поднимает **Caddy в Docker** (`/opt/caddy`, `docker compose`, как и
   remnanode) — никакого native-caddy/systemd;
2. открывает **TCP/80** для ACME-челленджа (HTTP-01) и `ufw allow 80`, если ufw
   активен;
3. получает **настоящий сертификат Let's Encrypt** на домен и отдаёт сайт на
   `127.0.0.1:9443` за proxy_protocol от xray;
4. заливает шаблон сайта в `/opt/caddy/html` и применяет хардненинг (без
   SPA-catch-all, стилизованная 404, кэш + security-заголовки, zstd);
5. дожидается выпуска сертификата и проверяет отдачу.

Полезные флаги:

```bash
sudo bash deploy/install.sh --dry-run      # сгенерировать и проверить конфиг, ничего не меняя
sudo bash deploy/install.sh --sync-only    # обновить только файлы сайта (после git pull)
sudo DOMAIN=example.com ACME_EMAIL=you@example.com bash deploy/install.sh --yes   # без вопросов
```

**Важное условие:** для выпуска сертификата Let's Encrypt нужен открытый
**TCP-порт 80** (HTTP-01) — TLS-ALPN на 443 невозможен, его занимает xray.
Установщик откроет 80 в `ufw`, но в облачной security-group порт нужно открыть
вручную.

Проверка после деплоя:

```bash
# реальный путь: 443 -> xray Reality -> selfsteal fallback
curl -skI --resolve hardrock.legendaryfm.uk:443:127.0.0.1 https://hardrock.legendaryfm.uk/ | head -n1
# сертификат должен быть Let's Encrypt, а не self-signed:
echo | openssl s_client -connect 127.0.0.1:9443 -servername hardrock.legendaryfm.uk 2>/dev/null | openssl x509 -noout -issuer
```

> Повторный запуск официального установщика selfsteal (DigneZzZ) перезапишет
> `/opt/caddy/Caddyfile` и вернёт SPA-catch-all — после такого reinstall просто
> запустите `deploy/install.sh` ещё раз, он восстановит хардненинг.

## Автодеплой при push (GitHub Actions)

В `.github/workflows/deploy.yml` настроена выкатка на сервер при каждом push в
`main` (синхронизация по SSH + `deploy/install.sh --sync-only`). Обновляются
**только файлы сайта** — конфиг Caddy, порты и сертификат не трогаются. Приватный
ключ живёт в GitHub Secrets и **не попадает в код/переписку**.

Настройка один раз:

1. Создайте **отдельный** deploy-ключ (не основной):
   ```bash
   ssh-keygen -t ed25519 -f deploy_key -C "github-deploy" -N ""
   ```
2. Публичную часть добавьте на сервер:
   ```bash
   cat deploy_key.pub >> /root/.ssh/authorized_keys
   ```
3. В GitHub: **Settings → Secrets and variables → Actions** добавьте секреты:
   - `SSH_HOST` — IP сервера
   - `SSH_USER` — `root`
   - `SSH_KEY` — содержимое приватного `deploy_key` (целиком)
   - `SSH_PORT` — опционально, если порт не 22
4. Один раз выполните полную установку вручную (`sudo bash deploy/install.sh`),
   далее каждый push в `main` будет обновлять файлы сайта автоматически.

> Ключ можно отозвать в любой момент, удалив строку из `authorized_keys` —
> доступ прекратится сразу, остальная конфигурация не затрагивается.

## Заметки по selfsteal-маскировке

- **Полная автономность.** Шрифты self-hosted (`assets/fonts/`), внешних
  запросов при загрузке страницы нет. Единственный исходящий трафик к третьим
  сторонам — это сами аудиопотоки, и только **после** клика по PLAY.
- **Уникализируйте перед деплоем.** Если несколько серверов используют один и
  тот же шаблон, decoy можно фингерпринтить по содержимому. Поменяйте название
  станции, тексты, палитру и порядок секций под каждый деплой.
- **Заголовки веб-сервера.** Отдавайте корректные `Content-Type`, `ETag`,
  `Last-Modified`, `Cache-Control` и HTTP/2 — как настоящий сайт. Сам HTML это
  не контролирует.
- **Логи.** Для маскировки и для соответствия заявлениям в Политике
  конфиденциальности отключите/анонимизируйте access-логи decoy (иначе текст о
  «несборе данных» становится недостоверным).

## Соответствие 152-ФЗ

- Персональные данные не собираются; форм ввода нет.
- Технические настройки (`theme_preference`, `cookie_consent`) хранятся в
  `localStorage` браузера и никуда не передаются — это **точно соответствует**
  тексту Политики конфиденциальности (`about.html#privacy`).
- Аналитика, рекламные сети и сторонние трекеры отсутствуют.
- Перед публичным размещением укажите реальные контакты оператора в разделе
  «О нас» и убедитесь, что серверные логи не противоречат заявленному.

## Технологии

Чистые HTML5 + CSS + ванильный JS. Без фреймворков, сборщиков и зависимостей
времени выполнения. Поддержка светлой/тёмной темы, адаптивная вёрстка,
доступность (ARIA, skip-link, фокус-стили, `prefers-reduced-motion`).

## Лицензия и права

Код шаблона — свободно используйте и изменяйте. Музыкальный контент и торговые
марки радиостанций принадлежат соответствующим вещателям и правообладателям;
проект лишь предоставляет ссылки на их публичные потоки.
